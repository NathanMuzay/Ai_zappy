host: 127.0.0.1
port: 3000
team_name: Br
seed: 42
model_path: models/zappy_ppo_Br.zip
timeout_seconds: 2.0
max_steps_per_episode: 2000
render_mode: none
episodes: 10
deterministic: true

# Doit correspondre a n_players dans agent_br.yaml : evalue les 4 joueurs
# de la politique partagee ensemble, pour pouvoir observer de vraies
# elevations de groupe (table page 5).
n_players: 4
