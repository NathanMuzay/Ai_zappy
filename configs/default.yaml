host: 127.0.0.1
port: 3000
team_name: Br
seed: 42
total_timesteps: 200000
model_path: models/zappy_ppo.zip
graph_path: data/zappy_training_rewards.png
progress_log_steps: 10000
timeout_seconds: 2.0
max_steps_per_episode: 2000
render_mode: none
episodes: 10
deterministic: true

algo:
  learning_rate: 0.0003
  n_steps: 1024
  batch_size: 64
  gamma: 0.99
  gae_lambda: 0.95
  ent_coef: 0.0
  n_epochs: 10